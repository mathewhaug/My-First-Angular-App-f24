import { TextColourDirective } from './text-colour.directive';

describe('TextColourDirective', () => {
  it('should create an instance', () => {
    const directive = new TextColourDirective();
    expect(directive).toBeTruthy();
  });
});
